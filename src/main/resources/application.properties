server.port=${PORT:8082}
spring.application.name=dashboard-service

# MongoDB — reads from env var, falls back to Atlas
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://Vercel-Admin-ChaturbujaPlots:QsDNB4d3fwNt4wV2@chaturbujaplots.np9kef5.mongodb.net/anjana_dashboard?retryWrites=true&w=majority}
spring.data.mongodb.database=anjana_dashboard

# JWT
app.jwt.secret=${APP_JWT_SECRET:DashB0@rdS3rv!ceK3y2025Anjana$ecret}
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,https://chaturbhujaplots-salestool-fe-customer-production.up.railway.app}

app.plot-service.url=${PLOT_SERVICE_URL:http://localhost:8083}
app.common-service.url=${COMMON_SERVICE_URL:http://localhost:8081}

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
